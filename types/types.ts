export type moviesType = {
    imdbID: string,
    Title:string,
    Poster:string,
    Year:string,
};

export type movieType = {
    imdbID: string,
    Title:string,
    Poster:string,
    Year:string,
    Plot: string,
    Actors: string,
    imdbRating: string,
}